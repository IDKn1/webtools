{
  "name": "webtools",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --minify",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-fetch": "^5.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.43.0"
  }
}
